# Frontend

Vanilla HTML/CSS/JS testing dashboard. No build step required.

Served automatically by FastAPI at `http://127.0.0.1:9200/` when the API is running.

## Files

- `index.html` — page layout with Request/Response sections
- `styles.css` — dark theme styling
- `app.js` — API client, session management, DOM rendering
